# Docker Basics

### Let's build a Docker image for our Python server

In the **Editor** locate the `gw-distsys-docker-introduction` directory and open the `Dockerfile`.

![](./assets/instructions.png)